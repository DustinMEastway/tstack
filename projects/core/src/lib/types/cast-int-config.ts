/** configuration for castInt method */
export interface CastIntConfig {
	/** @prop defaultValue to use if item is null */
	defaultValue?: boolean;
	/** @prop radix A value between 2 and 36 that specifies the base of the number */
	radix?: number;
}
